# Dev/Test Server Certificates

Pre-generated self-signed certificates for local TeddyCloud development.
Used by `make dev-sandbox-setup` to avoid the ~5 min RSA key generation on first run.

**Do not use in production.** These are for localhost testing only.
